<Window x:Class="Tetris.GameOver"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tetris"
        mc:Ignorable="d"
        Title="Game Over"
        Height="200" Width="300"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Background="Gold">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="🎉" FontSize="48" HorizontalAlignment="Center"/>
            <TextBlock Text="Game Over" FontSize="24" HorizontalAlignment="Center"/>
            <TextBlock Text="You scored" FontSize="16" HorizontalAlignment="Center"/>
            <TextBlock x:Name="ScoreText" Text="" FontSize="16" HorizontalAlignment="Center"/>
            <Button Content="OK" Width="60" Margin="0,20,0,0" 
                    HorizontalAlignment="Center" IsDefault="True"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
